name: LAPRAS Card
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-card:
    runs-on: ubuntu-latest
    steps:
      - uses: kawamataryo/lapras-card-readme@main
        with:
          SHARE_ID: "X74PLCX"
